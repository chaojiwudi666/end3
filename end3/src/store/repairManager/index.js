import reducers from './reducers';
import effects from './effects';

//3.2 定义store里面dispatch的字段
export const state = {
  
   
};

export default {
  name: 'repairManager',
  state,
  reducers,
  effects
};
